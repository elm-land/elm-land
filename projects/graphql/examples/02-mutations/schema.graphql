type Query {
  users: [User!]!
}

type Mutation {
  signUp(
    email: String!
    password: String!
    username: String
  ): User
}

type User {
  id: ID!
  email: String!
  username: String
  avatarUrl: String
}