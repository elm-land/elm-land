type Query {
  hello: String!
  person(id: ID): Person
}

type Mutation {
  person: PersonMutation!
}

type PersonMutation {
  create(
    name: String!
    email: String
  ): Person
}

type Person {
  id: ID!
  name: String!
  emailAddress: String
  followers: [Person!]!
}